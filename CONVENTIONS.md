# timetable規約

## 第1章 総則

### 第1条 目的と適用範囲

本規約は、このプロジェクトのコード・ドキュメント・チャットのすべてにかかる。人とAIエージェントを区別しない。

本規約が `docs/` と書くとき、それは親リポジトリ https://github.com/takayanag-i/timetable の `docs/` を指す。子リポジトリからは相対パスで解決しない。

### 第2条 事象・要件・仕様・実装

この4領域を区別し、他領域の内容を染み出させない。文書にもissueにもコードにもかかる。

| 領域 | 定義 | 正本 |
|------|------|---------|
| 事象 | 実際に観測されたふるまい。誰が何をしたときに何が起きたか | GitHub issues |
| 要件 | ユーザーのビジネス的な要求 | `docs/システム要件定義/` |
| 仕様 | システムの実際のふるまい | `docs/基本設計/` |
| 実装 | 実際のプログラムのふるまい | ソースコード |

事象は観測であって判断ではない。 壊れているとか、こうすべきとかは事象ではない。事象に含めてよいのは、操作・観測結果・再現条件だけ。原因も直し方も事象の外にある。

この区別は障害切り分けの前提である。 事象と要件が食い違うなら要件の取りこぼし、要件と仕様が食い違うなら仕様バグ、仕様と実装が食い違うなら実装バグ。領域が混ざった文書では、どのバグか判定できなくなる。

事象だけを見て直さない。 事象から必ず要件・仕様へ遡り、どこが食い違っているかを先に定める。遡らずに直すと、要件にも仕様にも無い挙動を実装に足すことになる。

染み出しの例。 要件文書にAPI形式・画面項目を書く。仕様文書にクラス名・ライブラリ選定・アルゴリズムを書く。実装の都合を要件として書く。

### 第3条 設計の境界

基本設計と詳細設計、外部設計と内部設計は別の軸である。混ぜない。

| 軸 | 何を分けるか |
|---|---|
| 基本設計 / 詳細設計 | 工程の時間軸。いつ設計するか。あわせて契約を言う |
| 外部設計 / 内部設計 | 境界。系と外界の境に晒されるか、系の内に閉じるか |

境界はマネジメント単位で引く。 アプリの都合だけで決まらず、管理体制を含意する。マネジメント層やリーダー層が異なる系に対しては、境界を定義して外部設計が要る。同じ層が見ている範囲の中は内部設計でよい。

本プロジェクトのマネジメント単位は次の4つ。

| 単位 | 範囲 |
|---|---|
| front-app | UIとBFF |
| 最適化APIと推論API | `fastapi/` |
| 内部APIとアーキ主導 | `spring/` と全体の設計。チャットAPIもこの単位が持つ |
| インフラ | `infra/` |

単位をまたぐものが外部設計になる。単位の中に閉じるものは内部設計。利用者から見えるかどうかではない。front-appと内部APIの間のGraphQLは利用者に見えないが、単位をまたぐので外部設計である。

単位をまたぐ契約は正本を1つ持つ。 両側が同じ形を手で書いて、どちらが正かを決めないまま並べない。

正本に従っていることを機械が判定できる形にする。 人の目で揃えるという約束は、片側だけが変わった日に破れる。

生成できるなら生成する。 1つの宣言から両側を生成すれば、離れる余地そのものが無くなる。突き合わせる検査は離れたことを見つけるだけで、離れることは防げない。生成できる場面で検査を選ぶ理由は無い。

### 第4条 法源と管轄

規約は複数の文書に分かれる。各文書には定めてよい事項の範囲がある。範囲の外を定めた条文は効力を持たない。

| 文書 | 定めるもの | 定めてはならないもの |
|---|---|---|
| 本規約 | 全リポジトリにかかる原則。何をしてよいかの可否 | 言語ごとの書式。個々のリポジトリにしか無い事実 |
| `docs/CLAUDE.md` | docs配下の執筆規約 | コードの書き方 |
| 各リポジトリの `CLAUDE.md` | その言語・その単位の書式と配置。そのリポジトリ固有の事実 | 本規約が定めた可否を緩めること |
| `SKILL.md` | 手順 | 規約 |
| `docs/` の設計文書 | 仕様 | 実装 |
| ソースコード | 実装 | 仕様 |

### 第5条 抵触規則

管轄が違えば抵触しない。 両方が同時にかかる。`fastapi/CLAUDE.md` は公開クラスにdocstringを求め、本規約はコメントを許可制とする。この2つは争わない。書式はfastapiが、書いてよいかは第23条が、書いてよい内容は第25条が決める。

同じ事項について食い違えば本規約が優先する。 各リポジトリの `CLAUDE.md` は本規約を緩められない。

各リポジトリが本規約より厳しい定めを置いたときは、厳しいほうが適用される。 増やす側に手続きを課すのが本規約の姿勢であり、さらに絞るのはその姿勢に沿う。

抵触を見つけたら片方を消す。 緩い側を残して運用で吸収しない。どちらが正しいかを決めて、誤りのほうを削る。真実はプロジェクトに1つという原則は規約自身にもかかる。

### 第6条 解釈準則

条文が当該の場面を明示していないとき、次の順で読む。

第一に文言に当たる。 表と例示が一次の資料になる。

文言で決まらなければ趣旨に当たる。 本規約の条文はいずれも理由を書いている。理由が及ぶ範囲まで条文は及ぶ。

例示は限定列挙ではない。 第25条の表は類型を挙げたものであり、同じ理由が当たる別の形にも及ぶ。例に無いから許されるとは読まない。

趣旨の及ばない場面へ拡げない。 理由が当たらないところに条文を持ち込まない。第15条は、値が無いことが正常な場面には及ばない。

疑わしいときは増やさない側に倒す。 コメント・文書・依存・機構はすべて、増やす側に手続きを課している。判断がつかないなら足さない。

### 第7条 執行と改正

機械判定できる条文はフックとCIが執行する。止まった理由はメッセージが述べる。文書検査の判定は本規約に同梱する `doc-lint/lint.py` が正本。Bashの禁止判定は各リポジトリのフックが正本。

執行の有無は条文の効力を変えない。 フックが見ていない違反も違反である。

条文を変えるときは規約リポジトリへPRを出す。 運用や口頭の合意で条文を上書きしない。

## 第2章 応対

### 第8条 チャットでの応対

- 質問に文章で回答するときは、mdファイルを作成せず、チャットに表示すること
- sedコマンドは使用禁止
- 日本語で回答すること

### 第9条 スキルを使う

手順が定められている作業は、素のコマンドを推測せず、その手順に従う。どの作業に手順があるかは各リポジトリの `CLAUDE.md` が持つ。

### 第10条 残作業はissueにする

作業中に残作業・ギャップ・将来拡張が出たら、その場で `gh issue create` で登録する。docsやメモリにTODOリストを書かない。

課題の正本は親リポジトリ `takayanag-i/timetable` のGitHub issuesであり、他所に控えを置けばそこから腐る。

## 第3章 表現

### 第11条 アイコンを使わない

絵文字も装飾記号も置かない。意味は言葉で書く。見出しに飾りを付けない。表の可否は「可」「不可」と書く。

### 第12条 注釈を括弧で足さない

`（）` による補足を書かない。 コードにもdocsにもチャットにもかかる。

括弧は書き手を甘やかす。言い切る責任を負わずに情報を付け足せてしまうため、主張が曖昧なまま量だけが増える。読み手には、括弧の中身が重要なのか読み飛ばしてよいのかが分からない。

```
悪い例: /** 学校名を取得する（未登録・スキーマ未対応の内部APIでもnullで継続） */
悪い例: - 認証ガード: `private.tsx` の loader が Cookie 有無で redirect（署名検証は内部API側）
```

書くべきことなら本文として言い切る。言い切るほどでないなら、それは書かなくてよい情報。

```
良い例: /** 学校名を取得する。未登録・取得失敗はnullを返し、呼び出し側は継続できる */
良い例: - 認証ガード: `private.tsx` の loader が Cookie 有無で redirect する。JWTの署名検証は内部API側の責務
```

例示は括弧ではなく `例:` を独立させるか、コードブロックで示す。数式・記号・引用元の原表記など、括弧が記法そのものである場合は対象外。

コード中の括弧の省き方は第21条が定める。本条とは別の事項を扱う。

### 第13条 太字を使わない

`**` による強調を置かない。重要さは語順と言い切りで示す。強調が散ると、本当に効く一文が埋もれる。

どうしても太字にしたい箇所が出たら、理由を添えて利用者の許可を得る。断りなく使わない。

### 第14条 アピール文を書かない

自分の仕事を説明する文をコードや文書に残さない。何を直したか、なぜ直したか、どう改善したかはコミットメッセージが持つ。読み手が必要とするのは現在の姿だけ。

## 第4章 書き方

いずれも読み手が全体を一目で捉えられるかを基準にしている。手続きを追わないと分からない書き方を避ける。言語ごとの書式は各リポジトリの `CLAUDE.md` が定める。

### 第15条 フォールバックしない

おかしいときはエラーにする。 値が取れない、前提が崩れている、想定外の状態にある。そのとき既定値・空・`undefined` に落として先へ進めてはならない。落とせばその場は動くが、誤りが表に出ないまま別の場所で意味不明な結果になる。

```java
悪い例: AuthHelper.getSchoolId().flatMap(repository::findById).orElse(null)
良い例: AuthHelper.getSchoolId().orElseThrow(() -> BusinessException.required("schoolId"))
```

- 「0件」と「取得できなかった」は違う。前者は正常系、後者は異常
- 握りつぶした `catch` を書かない。握るなら、なぜ握ってよいかを書く
- 環境や設定によって検証を飛ばす分岐を作らない。飛ばしたい理由があるなら、その設定自体を無くす

以下の各条は本条の系にあたる。第19条と第20条は、何が起きたかを呼び出し側から隠さないための形を定める。

### 第16条 分岐は集合として書く

分岐対象が閉じた集合なら、集合であることが見える形にする。`if` を並べると、どの場合を尽くしたかが読み手にも処理系にも分からない。

| 言語 | 形 |
|---|---|
| Java | `switch` 式。enumに対しては`default`を書かず、網羅をコンパイラに検査させる |
| TypeScript | キーを閉じたユニオンにした `Record<Key, handler>`。`switch` の `default` は未知の値を黙って通すため、表のほうが強い |
| Python | `match` 文、または辞書へのディスパッチ |

真偽値・範囲・早期リターンのような、集合でない分岐に当てはめない。`if` を消すこと自体が目的ではない。

### 第17条 反復は式として書く

添字で回す `for` を書かない。ループ変数の書き換えと境界条件が読み手の負担になる。

| 言語 | 形 |
|---|---|
| Java | `stream` / 拡張for |
| TypeScript | `map` / `filter` / `reduce` |
| Python | 内包表記 |

例外は、途中で打ち切る場合と、1回の走査で複数の集約を作る場合。`stream` に押し込むと読みにくくなるならforでよい。

### 第18条 副作用を減らす

引数で受け取ったオブジェクトを書き換えず、新しい値を返す。副作用が無ければ、テストは入力と出力だけで書ける。

永続化層も例外にしない。 書き込みは明示する。読んだ行を黙って書き換え、境界で反映される作りにしない。方針は[実装/README](https://github.com/takayanag-i/timetable/blob/main/docs/実装/README.md)3章。

### 第19条 voidを減らす

`void` のメソッドは、必ず副作用で仕事をする。何が起きたかを呼び出し側から隠すため、結果を検査できない。何を返せるか先に考える。

削除・通知のように結果を持たない操作は `void` でよい。

### 第20条 nullチェックよりOptional

戻り値で「無いかもしれない」を表すときは `Optional` を返し、呼び出し側に扱いを強制する。`null` を返すと、検査を忘れてもコンパイルが通る。

`Optional` を引数とフィールドに使わない。詳細と`orElseThrow`の使い分けは [内部APIの `CLAUDE.md`](https://github.com/takayanag-i/timetable-internal-api/blob/main/CLAUDE.md) が持つ。

TypeScriptには `Optional` が無いため、`T | null` を返して呼び出し側で絞り込むか、その場で `throw` する。

### 第21条 意味を変えない括弧を書かない

読む量が増えるだけで、どれが必要な括弧かが埋もれる。

- ラムダの引数が1つなら括弧を付けない。`x -> ...` / `x => ...`
- `return`の値を括弧で囲まない。折り返しのために必要な場合だけ使う
- 演算子の優先順位が自明なら括弧を足さない。自明でないなら括弧より変数に名前を付けて分ける

散文への注釈は第12条が定める。本条とは別の事項を扱う。

### 第22条 並び順は全順序にする

画面等でエンティティを並べるときは原則としてソート順を明示し、要件キーで同着になる場合はエンティティごとの比較キーで必ず決着させる。業務上意味のあるカラムを優先し、`id`/`ttid` での決着は業務一意キーが無い場合の最終手段。エンティティごとの比較キーは各[エンティティ文書](https://github.com/takayanag-i/timetable/blob/main/docs/基本設計/データ設計/エンティティ/README.md)の並び順が正本。

内部APIが並べていても画面のソートを省かない。 画面は取得した配列をそのまま出すとは限らない。追加した行をその場で差し込む、絞り込んだ結果を並べ直す、入れ子で取ったコレクションを使うといった経路では、取得時の並びが保たれない。両方で明示する。

## 第5章 コメント

### 第23条 許可制

コメントを書くときは、書く前に理由を添えて利用者の許可を得る。 断りなく書かない。

既定は書かないこと。 コメントは放っておくと増える。1つ1つは正しく見えても、積もると本文が埋もれ、コードを直すたびに嘘になる候補が増える。増やす側に手続きを課して、初めて釣り合う。

許可を求めるときは、コメントの文面と、それがコードから読み取れない理由を示す。理由を1文で言えないなら、それは書かなくてよい情報。

既にあるコメントを直すのは許可を要らない。 嘘になったものを直す、消すのは常にしてよい。増やすときだけ許可が要る。

### 第24条 唯一の判断基準

コードを読んでも分からないことだけを書く。

コードは「何を」を語れる。語れないのは「なぜそうしたか」。採らなかった選択肢、外から来た制約、直感に反する挙動の回避。これだけがコメントの持ち場。

```
良い例: // 全学級の一括描画は初期表示を遅くするため、初期は先頭学年のみ描画する
悪い例: // 講座IDから講座名を取得
```

### 第25条 書いてはならないもの

| 種別 | 例 | なぜ |
|---|---|---|
| 変更の経緯 | 「〜に統一した」「従来は〜だった」「移植漏れを修正」 | gitが持つ。コードは現在の姿だけを語る |
| 禁止の再掲 | 「各画面で書き直さない」「〜は持たない」 | 規約は本規約が持つ。読み手に守らせたいならレビューで言う |
| 名前の言い換え | `/** 学校名を取得する */ getSchoolName()` | 名前が悪いならコメントでなく名前を直す |
| 他ファイルの実装詳細 | 「Xxxサービスのfoo()と同じ手順」「Barから呼ばれている」 | 相手を変えると嘘になる。呼び出し元はgrepで足りる |
| 正本の指し直し | 「正本は docs/…/認可.md」「検査の中身はdoc-lintスキルが持つ」 | 正本は定めた場所が持つ。指し直せば二重管理になる |
| issue番号・日付・担当者 | `// #178 対応` | issueとgitが持つ。番号は再利用され意味を失う |

責務境界は書いてよい。 「このモジュールが担わないこと」は、読み手が探し先を誤らないための情報であって実装詳細ではない。たとえば「失効時のサイレントリフレッシュは auth-middleware が担う」。

### 第26条 更新するとき

コードを直したらコメントも直す。 嘘のコメントは無いより悪い。直すのが面倒なら、それは陳腐化するコメントを書いた証拠なので消す。

既存のコメントを増やす方向に手を入れない。 修正の説明をコメントで足すのは、コミットメッセージに書くべきことをコードに書いている。
