# timetable規約 17のゴール

## 前文

本規約は、このプロジェクトのコード・ドキュメント・チャットのすべてにかかる。人とAIエージェントを区別しない。

本規約が `docs/` と書くとき、それは親リポジトリ https://github.com/takayanag-i/timetable の `docs/` を指す。子リポジトリからは相対パスで解決しない。

17のゴールに分ける。 各ゴールはターゲットを持ち、`3.2` のように番号で指す。末尾が英字のターゲットは、そのゴールを実現する手立てを述べる。

---

## GOAL 1 領域を混ぜないようにしよう

事象・要件・仕様・実装の4領域を区別する。混ざった文書では、どこが食い違っているかを判定できなくなる。

| 領域 | 定義 | 正本 |
|------|------|---------|
| 事象 | 実際に観測されたふるまい。誰が何をしたときに何が起きたか | GitHub issues |
| 要件 | ユーザーのビジネス的な要求 | `docs/システム要件定義/` |
| 仕様 | システムの実際のふるまい | `docs/基本設計/` |
| 実装 | 実際のプログラムのふるまい | ソースコード |

- 1.1 事象に書いてよいのは、操作・観測結果・再現条件だけ。原因も直し方も事象の外にある
- 1.2 事象と要件が食い違うなら要件の取りこぼし、要件と仕様が食い違うなら仕様バグ、仕様と実装が食い違うなら実装バグ。どのバグかを先に定める
- 1.3 事象だけを見て直さない。事象から要件・仕様へ遡り、どこが食い違っているかを先に定める。遡らずに直すと、要件にも仕様にも無い挙動を実装に足すことになる
- 1.4 要件文書にAPI形式・画面項目を書かない。仕様文書にクラス名・ライブラリ選定・アルゴリズムを書かない。実装の都合を要件として書かない

## GOAL 2 境界はマネジメント単位で引こう

基本設計と詳細設計、外部設計と内部設計は別の軸である。混ぜない。

| 軸 | 何を分けるか |
|---|---|
| 基本設計 / 詳細設計 | 工程の時間軸。いつ設計するか。あわせて契約を言う |
| 外部設計 / 内部設計 | 境界。系と外界の境に晒されるか、系の内に閉じるか |

本プロジェクトのマネジメント単位は次の5つ。

| 単位 | 範囲 |
|---|---|
| front-app | UIとBFF |
| 最適化APIと推論API | `fastapi/` |
| 内部API | `spring/`。チャットAPIもこの単位が持つ |
| インフラ | `infra/` |
| 帳票 | 帳票と集計 |

- 2.1 境界はマネジメント単位で引く。マネジメント層やリーダー層が異なる系に対しては、境界を定義して外部設計が要る。同じ層が見ている範囲の中は内部設計でよい
- 2.2 単位をまたぐものが外部設計になる。単位の中に閉じるものは内部設計。利用者から見えるかどうかではない。front-appと内部APIの間のGraphQLは利用者に見えないが、単位をまたぐので外部設計である
- 2.3 全体の設計は単位に属さない。統括が直に持つ。単位の間に境界を引く役が、その境界のどちらかに立つことはできない
- 2.4 リポジトリは単位に1つ。単位の中では実行単位をいくつ分けてもよい

## GOAL 3 契約は生成しよう

単位をまたぐ契約は、両側が同じ形を手で書いて、どちらが正かを決めないまま並べることをしない。

- 3.1 単位をまたぐ契約は正本を1つ持つ
- 3.2 正本に従っていることを機械が判定できる形にする。人の目で揃えるという約束は、片側だけが変わった日に破れる
- 3.3 生成できるなら生成する。1つの宣言から両側を生成すれば、離れる余地そのものが無くなる。突き合わせる検査は離れたことを見つけるだけで、離れることは防げない。生成できる場面で検査を選ぶ理由は無い

## GOAL 4 どの文書が何を決めるか、はっきりさせよう

規約は複数の文書に分かれる。各文書には定めてよい事項の範囲がある。範囲の外を定めた条文は効力を持たない。

| 文書 | 定めるもの | 定めてはならないもの |
|---|---|---|
| 本規約 | 全リポジトリにかかる原則。何をしてよいかの可否 | 言語ごとの書式。個々のリポジトリにしか無い事実 |
| `docs/CLAUDE.md` | docs配下の執筆規約 | コードの書き方 |
| 各リポジトリの `CLAUDE.md` | その言語・その単位の書式と配置。そのリポジトリ固有の事実 | 本規約が定めた可否を緩めること |
| `SKILL.md` | 手順 | 規約 |
| `docs/` の設計文書 | 仕様 | 実装 |
| ソースコード | 実装 | 仕様 |

- 4.1 管轄が違えば抵触しない。両方が同時にかかる。`fastapi/CLAUDE.md` は公開クラスにdocstringを求め、本規約はコメントを許可制とする。この2つは争わない。書式はfastapiが、書いてよいかはGOAL 17が、書いてよい内容は17.3が決める
- 4.2 同じ事項について食い違えば本規約が優先する。各リポジトリの `CLAUDE.md` は本規約を緩められない
- 4.3 各リポジトリが本規約より厳しい定めを置いたときは、厳しいほうが適用される。増やす側に手続きを課すのが本規約の姿勢であり、さらに絞るのはその姿勢に沿う
- 4.4 抵触を見つけたら片方を消す。緩い側を残して運用で吸収しない。どちらが正しいかを決めて、誤りのほうを削る

## GOAL 5 条文は趣旨から読もう

ターゲットが当該の場面を明示していないとき、次の順で読む。

- 5.1 第一に文言に当たる。表と例示が一次の資料になる
- 5.2 文言で決まらなければ趣旨に当たる。本規約のターゲットはいずれも理由を書いている。理由が及ぶ範囲までターゲットは及ぶ
- 5.3 例示は限定列挙ではない。17.3の表は類型を挙げたものであり、同じ理由が当たる別の形にも及ぶ。例に無いから許されるとは読まない
- 5.4 趣旨の及ばない場面へ拡げない。理由が当たらないところにターゲットを持ち込まない。GOAL 13は、値が無いことが正常な場面には及ばない
- 5.5 疑わしいときは増やさない側に倒す。コメント・文書・依存・機構はすべて、増やす側に手続きを課している。判断がつかないなら足さない

## GOAL 6 機械に守らせよう

- 6.1 機械判定できるターゲットはフックとCIが執行する。止まった理由はメッセージが述べる
- 6.2 文書検査の判定は本規約に同梱する `doc-lint/lint.py` が正本。Bashの禁止判定は各リポジトリのフックが正本
- 6.3 執行の有無はターゲットの効力を変えない。フックが見ていない違反も違反である
- 6.a ターゲットを変えるときは規約リポジトリへPRを出す。運用や口頭の合意でターゲットを上書きしない

## GOAL 7 日本語で、その場で答えよう

- 7.1 日本語で回答する
- 7.2 質問に文章で回答するときは、mdファイルを作成せず、チャットに表示する
- 7.3 sedコマンドは使用禁止

## GOAL 8 決まった手順に従おう

- 8.1 手順が定められている作業は、素のコマンドを推測せず、その手順に従う
- 8.2 どの作業に手順があるかは各リポジトリの `CLAUDE.md` が持つ

## GOAL 9 残作業はissueにしよう

- 9.1 作業中に残作業・ギャップ・将来拡張が出たら、その場で `gh issue create` で登録する
- 9.2 docsやメモリにTODOリストを書かない
- 9.3 課題の正本は親リポジトリ `takayanag-i/timetable` のGitHub issues。他所に控えを置けばそこから腐る

## GOAL 10 飾らずに書こう

意味は言葉で書く。飾りが散ると、本当に効く一文が埋もれる。

- 10.1 絵文字も装飾記号も置かない。見出しに飾りを付けない
- 10.2 表の可否は「可」「不可」と書く
- 10.3 `**` による強調を置かない。重要さは語順と言い切りで示す
- 10.a どうしても太字にしたい箇所が出たら、理由を添えて利用者の許可を得る。断りなく使わない

## GOAL 11 言い切ろう

括弧は書き手を甘やかす。言い切る責任を負わずに情報を付け足せてしまうため、主張が曖昧なまま量だけが増える。読み手には、括弧の中身が重要なのか読み飛ばしてよいのかが分からない。

- 11.1 `（）` による補足を書かない。コードにもdocsにもチャットにもかかる

```
悪い例: /** 学校名を取得する（未登録・スキーマ未対応の内部APIでもnullで継続） */
良い例: /** 学校名を取得する。未登録・取得失敗はnullを返し、呼び出し側は継続できる */
```

- 11.2 書くべきことなら本文として言い切る。言い切るほどでないなら、それは書かなくてよい情報
- 11.3 例示は括弧ではなく `例:` を独立させるか、コードブロックで示す。数式・記号・引用元の原表記など、括弧が記法そのものである場合は対象外
- 11.4 コード中も、意味を変えない括弧を書かない。ラムダの引数が1つなら括弧を付けない。`return` の値を括弧で囲まない。演算子の優先順位が自明なら括弧を足さない。自明でないなら括弧より変数に名前を付けて分ける
- 11.5 自分の仕事を説明する文をコードや文書に残さない。何を直したか、なぜ直したか、どう改善したかはコミットメッセージが持つ。読み手が必要とするのは現在の姿だけ

## GOAL 12 肯定で定めよう

規範は、名宛人が何をするかで書く。対象は何であるか、いくつあるか、どこまで及ぶか。これらを積極的に画く。

否定は、書き手には楽で読み手には重い。 「〜しない」と書けば、読み手は残りが何かを自分で数え上げることになる。数え上げた結果が書き手の意図と一致する保証はどこにもない。

- 12.1 数量は基数で書く。上限を禁止で示さず、成り立つ数そのものを述べる

```
悪い例: 1つのオブジェクトが2つのメッセージに付くことはない
良い例: 1つのオブジェクトは1つのメッセージに付く
```

- 12.2 適用範囲は限定列挙で画く。何が対象かを挙げれば、挙げていないものは規定の外にある。外にあることを重ねて述べない

```
悪い例: リクエストとレスポンスのボディは採番しない。単位の中で閉じる呼び出しは採番しない
良い例: 採番するのは、単位をまたぐ呼び出しの入口である
```

法令が定義規定を先に置くのと同じ形である。定義が対象を画し、そこに入らないものは条文が触れない。触れないことを条文に書けば、定義が対象を画いていないと告白することになる。

- 12.3 状態は成り立つ姿で書く。「本文を持たない」ではなく、何を持つかを書く。空であることも1つの姿である
- 12.4 禁止規定は人の行為を縛るときに限る。GOAL 7・GOAL 10・GOAL 11・GOAL 17がこれにあたる。名宛人が人だから禁止の形を採る。系のふるまいを述べる文書には置かない。系は規範に従うのではなく、書かれたとおりに動く。動かない形を並べても、動く形は定まらない
- 12.5 積極的な範囲で切り出せないものだけを例外として書く。外れるものを列挙できるなら、原則の側を書き直す
- 12.6 状態と応答の対応を並べた表は、否定ではない。系がどの状態で何を返すかを述べたものであり、GOAL 13が誤りを表に出すことを求める以上、何を返すかは書く必要がある
- 12.7 設計判断の理由は、比較の形でしか書けないことがある。そのときは比較を書いてよい。採らなかった案を並べるのではなく、採った案の性質を説明する1つの比較に限る

## GOAL 13 おかしいときはエラーにしよう

値が取れない、前提が崩れている、想定外の状態にある。そのとき既定値・空・`undefined` に落として先へ進めない。落とせばその場は動くが、誤りが表に出ないまま別の場所で意味不明な結果になる。

```java
悪い例: AuthHelper.getSchoolId().flatMap(repository::findById).orElse(null)
良い例: AuthHelper.getSchoolId().orElseThrow(() -> BusinessException.required("schoolId"))
```

- 13.1 「0件」と「取得できなかった」は違う。前者は正常系、後者は異常
- 13.2 握りつぶした `catch` を書かない。握るなら、なぜ握ってよいかを書く
- 13.3 環境や設定によって検証を飛ばす分岐を作らない。飛ばしたい理由があるなら、その設定自体を無くす
- 13.4 表せない形にできるなら、そのほうが強い。検査は起きたことを見つけるだけで、起きることは防げない

## GOAL 14 集合として、式として書こう

読み手が全体を一目で捉えられるかを基準にする。手続きを追わないと分からない書き方を避ける。

分岐対象が閉じた集合なら、集合であることが見える形にする。`if` を並べると、どの場合を尽くしたかが読み手にも処理系にも分からない。

| 言語 | 分岐の形 |
|---|---|
| Java | `switch` 式。enumに対しては`default`を書かず、網羅をコンパイラに検査させる |
| TypeScript | キーを閉じたユニオンにした `Record<Key, handler>` |
| Python | `match` 文、または辞書へのディスパッチ |

| 言語 | 反復の形 |
|---|---|
| Java | `stream` / 拡張for |
| TypeScript | `map` / `filter` / `reduce` |
| Python | 内包表記 |

- 14.1 真偽値・範囲・早期リターンのような、集合でない分岐に当てはめない。`if` を消すこと自体が目的ではない
- 14.2 添字で回す `for` を書かない。ループ変数の書き換えと境界条件が読み手の負担になる
- 14.3 反復の例外は、途中で打ち切る場合と、1回の走査で複数の集約を作る場合。`stream` に押し込むと読みにくくなるならforでよい

## GOAL 15 何が起きたかを隠さないようにしよう

- 15.1 引数で受け取ったオブジェクトを書き換えず、新しい値を返す。副作用が無ければ、テストは入力と出力だけで書ける
- 15.2 永続化層も例外にしない。書き込みは明示する。読んだ行を黙って書き換え、境界で反映される作りにしない
- 15.3 `void` のメソッドは、必ず副作用で仕事をする。何が起きたかを呼び出し側から隠すため、結果を検査できない。何を返せるか先に考える。削除・通知のように結果を持たない操作は `void` でよい
- 15.4 戻り値で「無いかもしれない」を表すときは `Optional` を返し、呼び出し側に扱いを強制する。`null` を返すと、検査を忘れてもコンパイルが通る
- 15.5 `Optional` を引数とフィールドに使わない。TypeScriptには `Optional` が無いため、`T | null` を返して呼び出し側で絞り込むか、その場で `throw` する

## GOAL 16 並び順は最後まで決めよう

- 16.1 画面等でエンティティを並べるときはソート順を明示し、要件キーで同着になる場合はエンティティごとの比較キーで必ず決着させる
- 16.2 業務上意味のあるカラムを優先する。`id`/`ttid` での決着は業務一意キーが無い場合の最終手段
- 16.3 エンティティごとの比較キーは各エンティティ文書の並び順が正本
- 16.4 内部APIが並べていても画面のソートを明示する。追加した行をその場で差し込む、絞り込んだ結果を並べ直す、入れ子で取ったコレクションを使うといった経路では、取得時の並びが保たれない

## GOAL 17 コメントは、書く前に相談しよう

既定は書かないこと。コメントは放っておくと増える。1つ1つは正しく見えても、積もると本文が埋もれ、コードを直すたびに嘘になる候補が増える。増やす側に手続きを課して、初めて釣り合う。

- 17.1 コメントを書くときは、書く前に理由を添えて利用者の許可を得る。許可を求めるときは、コメントの文面と、それがコードから読み取れない理由を示す。理由を1文で言えないなら、それは書かなくてよい情報
- 17.2 コードを読んでも分からないことだけを書く。コードは「何を」を語れる。語れないのは「なぜそうしたか」。採らなかった選択肢、外から来た制約、直感に反する挙動の回避。これだけがコメントの持ち場

```
良い例: // 全学級の一括描画は初期表示を遅くするため、初期は先頭学年のみ描画する
悪い例: // 講座IDから講座名を取得
```

- 17.3 次の6種は書かない

| 種別 | 例 | なぜ |
|---|---|---|
| 変更の経緯 | 「〜に統一した」「従来は〜だった」「移植漏れを修正」 | gitが持つ。コードは現在の姿だけを語る |
| 禁止の再掲 | 「各画面で書き直さない」「〜は持たない」 | 規約は本規約が持つ。読み手に守らせたいならレビューで言う |
| 名前の言い換え | `/** 学校名を取得する */ getSchoolName()` | 名前が悪いならコメントでなく名前を直す |
| 他ファイルの実装詳細 | 「Xxxサービスのfoo()と同じ手順」「Barから呼ばれている」 | 相手を変えると嘘になる。呼び出し元はgrepで足りる |
| 正本の指し直し | 「正本は docs/…/認可.md」「検査の中身はdoc-lintスキルが持つ」 | 正本は定めた場所が持つ。指し直せば二重管理になる |
| issue番号・日付・担当者 | `// #178 対応` | issueとgitが持つ。番号は再利用され意味を失う |

- 17.4 責務境界は書いてよい。「このモジュールが担わないこと」は、読み手が探し先を誤らないための情報であって実装詳細ではない
- 17.5 コードを直したらコメントも直す。嘘のコメントは無いより悪い。直すのが面倒なら、それは陳腐化するコメントを書いた証拠なので消す
- 17.6 既にあるコメントを直すのは許可を要らない。嘘になったものを直す、消すのは常にしてよい。増やすときだけ許可が要る
- 17.7 既存のコメントを増やす方向に手を入れない。修正の説明をコメントで足すのは、コミットメッセージに書くべきことをコードに書いている
