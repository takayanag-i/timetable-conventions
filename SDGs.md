# Sustainable Development Goals

## 前文

本規約は、このプロジェクトのコード・ドキュメント・チャットのすべてにかかる。人とAIエージェントを区別しない。

本規約が `docs/` と書くとき、それは親リポジトリ https://github.com/takayanag-i/timetable の `docs/` を指す。子リポジトリからは相対パスで解決しない。

12のゴールに分ける。 各ゴールはターゲットを持ち、`3.2` のように番号で指す。末尾が英字のターゲットは、そのゴールを実現する手立てを述べる。

### 応対

日本語で回答する。

質問に文章で回答するときは、mdファイルを作成せず、チャットに表示する。

sedコマンドは使用禁止。

### 読み方

ターゲットが当該の場面を明示していないとき、次の順で読む。

第一に文言に当たる。 表と例示が一次の資料になる。

文言で決まらなければ趣旨に当たる。 本規約のターゲットはいずれも理由を書いている。理由が及ぶ範囲までターゲットは及ぶ。

例示は限定列挙ではない。 同じ理由が当たる別の形にも及ぶ。例に無いから許されるとは読まない。

趣旨の及ばない場面へ拡げない。 理由が当たらないところにターゲットを持ち込まない。GOAL 7 は、値が無いことが正常な場面には及ばない。

---

## GOAL 1 ボーダーでは約束をしよう

境界の内側は自由でよい。境界をまたぐものだけが約束になる。どこに境界があるかを先に定め、またぐものに正本を1つ与える。

本プロジェクトのマネジメント単位は次の5つ。

| 単位 | 範囲 |
|---|---|
| front-app | UIとBFF |
| 最適化APIと推論API | `fastapi/` |
| 内部API | `spring/`。チャットAPIもこの単位が持つ |
| インフラ | `infra/` |
| 帳票 | 帳票と集計 |

基本設計と詳細設計、外部設計と内部設計は別の軸である。混ぜない。

| 軸 | 何を分けるか |
|---|---|
| 基本設計 / 詳細設計 | 工程の時間軸。いつ設計するか。あわせて契約を言う |
| 外部設計 / 内部設計 | 境界。系と外界の境に晒されるか、系の内に閉じるか |

- 1.1 境界はマネジメント単位で引く。マネジメント層やリーダー層が異なる系に対しては、境界を定義して外部設計が要る。同じ層が見ている範囲の中は内部設計でよい
- 1.2 単位をまたぐものが外部設計になる。単位の中に閉じるものは内部設計。利用者から見えるかどうかではない。front-appと内部APIの間のGraphQLは利用者に見えないが、単位をまたぐので外部設計である
- 1.3 全体の設計は単位に属さない。統括が直に持つ。単位の間に境界を引く役が、その境界のどちらかに立つことはできない
- 1.4 リポジトリは単位に1つ。単位の中では実行単位をいくつ分けてもよい
- 1.5 単位をまたぐ契約は正本を1つ持つ
- 1.6 正本に従っていることを機械が判定できる形にする。人の目で揃えるという約束は、片側だけが変わった日に破れる
- 1.7 生成できるなら生成する。1つの宣言から両側を生成すれば、離れる余地そのものが無くなる。突き合わせる検査は離れたことを見つけるだけで、離れることは防げない。生成できる場面で検査を選ぶ理由は無い

## GOAL 2 ことばにこだわろう

同じ語で違うものを指すと、どこが食い違っているかを判定できなくなる。

- 2.1 事象・要件・仕様・実装の4領域を区別する

| 領域 | 定義 | 正本 |
|------|------|---------|
| 事象 | 実際に観測されたふるまい。誰が何をしたときに何が起きたか | GitHub issues |
| 要件 | ユーザーのビジネス的な要求 | `docs/システム要件定義/` |
| 仕様 | システムの実際のふるまい | `docs/基本設計/` |
| 実装 | 実際のプログラムのふるまい | ソースコード |

事象に書いてよいのは、操作・観測結果・再現条件だけ。 原因も直し方も事象の外にある。

- 2.2 障害はこの区別で切り分ける。事象と要件が食い違うなら要件の取りこぼし、要件と仕様が食い違うなら仕様バグ、仕様と実装が食い違うなら実装バグ。どのバグかを先に定める

事象だけを見て直さない。 事象から要件・仕様へ遡り、どこが食い違っているかを先に定める。遡らずに直すと、要件にも仕様にも無い挙動を実装に足すことになる。

- 2.3 工程の言葉を隣の工程へ染み出させない。要件文書にAPI形式・画面項目を書かない。仕様文書にクラス名・ライブラリ選定・アルゴリズムを書かない。実装の都合を要件として書かない

## GOAL 3 真実はいつもひとつ

同じ事実を複数の場所に書かない。事実ごとに正本を1箇所に定め、他からは参照する。

各文書には定めてよい事項の範囲がある。範囲の外を定めた記述は効力を持たない。

| 文書 | 定めるもの | 定めてはならないもの |
|---|---|---|
| 本規約 | 全リポジトリにかかる原則。何をしてよいかの可否 | 言語ごとの書式。個々のリポジトリにしか無い事実 |
| `docs/CLAUDE.md` | docs配下の執筆規約 | コードの書き方 |
| 各リポジトリの `CLAUDE.md` | その言語・その単位の書式と配置。そのリポジトリ固有の事実 | 本規約が定めた可否を緩めること |
| `SKILL.md` | 手順 | 規約 |
| `docs/` の設計文書 | 仕様 | 実装 |
| ソースコード | 実装 | 仕様 |

- 3.1 再掲するときは引用元を明記し、正本がどこかを読者が判別できるようにする
- 3.2 正本の所在はフロントマターが持つ。本文に「正本は〇〇」と書かない。それ自体が二重管理になる。この文書が扱う仕様の正本は `source_of_truth`、上流と下流は `input_documents` と `output_documents` が持つ
- 3.3 管轄が違えば抵触しない。両方が同時にかかる。`fastapi/CLAUDE.md` は公開クラスにdocstringを求め、本規約はコメントを許可制とする。この2つは争わない。書式はfastapiが、書いてよいかは GOAL 12 が決める
- 3.4 同じ事項について食い違えば本規約が優先する。各リポジトリの `CLAUDE.md` は本規約を緩められない
- 3.5 各リポジトリが本規約より厳しい定めを置いたときは、厳しいほうが適用される。増やす側に手続きを課すのが本規約の姿勢であり、さらに絞るのはその姿勢に沿う
- 3.6 抵触を見つけたら片方を消す。緩い側を残して運用で吸収しない。どちらが正しいかを決めて、誤りのほうを削る

## GOAL 4 しくみで守ろう

約束は人の記憶に置かない。機械が判定できるものは機械に、手順があるものは手順に載せる。

- 4.1 機械判定できるターゲットはフックとCIが執行する。止まった理由はメッセージが述べる
- 4.2 文書検査の判定は本規約に同梱する `doc-lint/lint.py` が正本。Bashの禁止判定は各リポジトリのフックが正本
- 4.3 執行の有無はターゲットの効力を変えない。フックが見ていない違反も違反である
- 4.4 手順が定められている作業は、素のコマンドを推測せず、その手順に従う。どの作業に手順があるかは各リポジトリの `CLAUDE.md` が持つ
- 4.a ターゲットを変えるときは規約リポジトリへPRを出す。運用や口頭の合意でターゲットを上書きしない

## GOAL 5 イシューを残そう

- 5.1 作業中に残作業・ギャップ・将来拡張が出たら、その場で `gh issue create` で登録する
- 5.2 docsやメモリにTODOリストを書かない
- 5.3 課題の正本は親リポジトリ `takayanag-i/timetable` のGitHub issues。他所に控えを置けばそこから腐る

## GOAL 6 かざらないシンプルなメッセージを

意味は言葉で書く。飾りが散ると、本当に効く一文が埋もれる。

- 6.1 絵文字も装飾記号も置かない。見出しに飾りを付けない。表の可否は「可」「不可」と書く
- 6.2 `**` による強調を置かない。重要さは語順と言い切りで示す
- 6.3 `（）` による補足を書かない。括弧は書き手を甘やかす。言い切る責任を負わずに情報を付け足せてしまうため、主張が曖昧なまま量だけが増える。読み手には、括弧の中身が重要なのか読み飛ばしてよいのかが分からない

```
悪い例: /** 学校名を取得する（未登録・スキーマ未対応の内部APIでもnullで継続） */
良い例: /** 学校名を取得する。未登録・取得失敗はnullを返し、呼び出し側は継続できる */
```

書くべきことなら本文として言い切る。 言い切るほどでないなら、それは書かなくてよい情報。例示は括弧ではなく `例:` を独立させるか、コードブロックで示す。数式・記号・引用元の原表記など、括弧が記法そのものである場合は対象外。

- 6.4 コード中も、意味を変えない括弧を書かない。ラムダの引数が1つなら括弧を付けない。`return` の値を括弧で囲まない。演算子の優先順位が自明なら括弧を足さない。自明でないなら括弧より変数に名前を付けて分ける
- 6.5 自分の仕事を説明する文をコードや文書に残さない。何を直したか、なぜ直したか、どう改善したかはコミットメッセージが持つ。読み手が必要とするのは現在の姿だけ
- 6.6 規範は、名宛人が何をするかで書く。対象は何であるか、いくつあるか、どこまで及ぶか。これらを積極的に画く。否定は、書き手には楽で読み手には重い。「〜しない」と書けば、読み手は残りが何かを自分で数え上げることになる
- 6.7 数量は基数で書く。上限を禁止で示さず、成り立つ数そのものを述べる

```
悪い例: 1つのオブジェクトが2つのメッセージに付くことはない
良い例: 1つのオブジェクトは1つのメッセージに付く
```

- 6.8 適用範囲は限定列挙で画く。何が対象かを挙げれば、挙げていないものは規定の外にある。外にあることを重ねて述べない

```
悪い例: リクエストとレスポンスのボディは採番しない。単位の中で閉じる呼び出しは採番しない
良い例: 採番するのは、単位をまたぐ呼び出しの入口である
```

法令が定義規定を先に置くのと同じ形である。 定義が対象を画し、そこに入らないものは条文が触れない。触れないことを条文に書けば、定義が対象を画いていないと告白することになる。

- 6.9 状態は成り立つ姿で書く。「本文を持たない」ではなく、何を持つかを書く。空であることも1つの姿である
- 6.10 禁止規定は人の行為を縛るときに限る。前文の応対、GOAL 6、GOAL 12 がこれにあたる。名宛人が人だから禁止の形を採る。系のふるまいを述べる文書には置かない。系は規範に従うのではなく、書かれたとおりに動く。動かない形を並べても、動く形は定まらない
- 6.11 積極的な範囲で切り出せないものだけを例外として書く。外れるものを列挙できるなら、原則の側を書き直す
- 6.12 状態と応答の対応を並べた表は、否定ではない。系がどの状態で何を返すかを述べたものであり、GOAL 7 が誤りを表に出すことを求める以上、何を返すかは書く必要がある
- 6.13 設計判断の理由は、比較の形でしか書けないことがある。そのときは比較を書いてよい。採らなかった案を並べるのではなく、採った案の性質を説明する1つの比較に限る

## GOAL 7 おかしいときはエラーにしよう

値が取れない、前提が崩れている、想定外の状態にある。そのとき既定値・空・`undefined` に落として先へ進めない。この落とし方をフォールバックと呼び、禁止する。 落とせばその場は動くが、誤りが表に出ないまま別の場所で意味不明な結果になる。

```java
悪い例: AuthHelper.getSchoolId().flatMap(repository::findById).orElse(null)
良い例: AuthHelper.getSchoolId().orElseThrow(() -> BusinessException.required("schoolId"))
```

- 7.1 「0件」と「取得できなかった」は違う。前者は正常系、後者は異常
- 7.2 握りつぶした `catch` を書かない。握るなら、なぜ握ってよいかを書く
- 7.3 環境や設定によって検証を飛ばす分岐を作らない。飛ばしたい理由があるなら、その設定自体を無くす
- 7.4 表せない形にできるなら、そのほうが強い。検査は起きたことを見つけるだけで、起きることは防げない

## GOAL 8 まとまりで宣言的に書こう

読み手が全体を一目で捉えられるかを基準にする。手続きを追わないと分からない書き方を避ける。

分岐対象が閉じた集合なら、集合であることが見える形にする。`if` を並べると、どの場合を尽くしたかが読み手にも処理系にも分からない。

| 言語 | 分岐の形 |
|---|---|
| Java | `switch` 式。enumに対しては`default`を書かず、網羅をコンパイラに検査させる |
| TypeScript | キーを閉じたユニオンにした `Record<Key, handler>` |
| Python | `match` 文、または辞書へのディスパッチ |

| 言語 | 反復の形 |
|---|---|
| Java | `stream` / 拡張for |
| TypeScript | `map` / `filter` / `reduce` |
| Python | 内包表記 |

- 8.1 真偽値・範囲・早期リターンのような、集合でない分岐に当てはめない。`if` を消すこと自体が目的ではない
- 8.2 添字で回す `for` を書かない。ループ変数の書き換えと境界条件が読み手の負担になる
- 8.3 反復の例外は、途中で打ち切る場合と、1回の走査で複数の集約を作る場合。`stream` に押し込むと読みにくくなるならforでよい

## GOAL 9 副作用をなくそう

- 9.1 引数で受け取ったオブジェクトを書き換えず、新しい値を返す。副作用が無ければ、テストは入力と出力だけで書ける
- 9.2 永続化層も例外にしない。書き込みは明示する。読んだ行を黙って書き換え、境界で反映される作りにしない
- 9.3 `void` のメソッドは、必ず副作用で仕事をする。何が起きたかを呼び出し側から隠すため、結果を検査できない。何を返せるか先に考える。削除・通知のように結果を持たない操作は `void` でよい

## GOAL 10 コンパイラを活用しよう

型が語れることを、実行時の検査や人の記憶に肩代わりさせない。

- 10.1 戻り値で「無いかもしれない」を表すときは `Optional` を返し、呼び出し側に扱いを強制する。`null` を返すと、検査を忘れてもコンパイルが通る
- 10.2 `Optional` を引数とフィールドに使わない。TypeScriptには `Optional` が無いため、`T | null` を返して呼び出し側で絞り込むか、その場で `throw` する
- 10.3 起こりえない値を型から落とす。返さない `null`、渡らない分岐。実行時に確かめる前に、表せない形にする

## GOAL 11 データディクショナリを育てよう

エンティティ文書が、そのエンティティについての事実を集める。属性・制約・比較キーがそこに揃う。

- 11.1 画面等でエンティティを並べるときはソート順を明示し、要件キーで同着になる場合はエンティティごとの比較キーで必ず決着させる
- 11.2 業務上意味のあるカラムを優先する。`id`/`ttid` での決着は業務一意キーが無い場合の最終手段
- 11.3 エンティティごとの比較キーは各エンティティ文書の並び順が正本
- 11.4 内部APIが並べていても画面のソートを明示する。追加した行をその場で差し込む、絞り込んだ結果を並べ直す、入れ子で取ったコレクションを使うといった経路では、取得時の並びが保たれない

## GOAL 12 1増やしたら3減らす

増やす側に手続きを課す。コメント・文書・依存・機構はすべて、放っておくと増える。

- 12.1 疑わしいときは増やさない側に倒す。判断がつかないなら足さない
- 12.2 コメントを書くときは、書く前に理由を添えて利用者の許可を得る。許可を求めるときは、コメントの文面と、それがコードから読み取れない理由を示す。理由を1文で言えないなら、それは書かなくてよい情報
- 12.3 コードを読んでも分からないことだけを書く。コードは「何を」を語れる。語れないのは「なぜそうしたか」。採らなかった選択肢、外から来た制約、直感に反する挙動の回避。これだけがコメントの持ち場

```
良い例: // 全学級の一括描画は初期表示を遅くするため、初期は先頭学年のみ描画する
悪い例: // 講座IDから講座名を取得
```

- 12.4 次の6種は書かない

| 種別 | 例 | なぜ |
|---|---|---|
| 変更の経緯 | 「〜に統一した」「従来は〜だった」「移植漏れを修正」 | gitが持つ。コードは現在の姿だけを語る |
| 禁止の再掲 | 「各画面で書き直さない」「〜は持たない」 | 規約は本規約が持つ。読み手に守らせたいならレビューで言う |
| 名前の言い換え | `/** 学校名を取得する */ getSchoolName()` | 名前が悪いならコメントでなく名前を直す |
| 他ファイルの実装詳細 | 「Xxxサービスのfoo()と同じ手順」「Barから呼ばれている」 | 相手を変えると嘘になる。呼び出し元はgrepで足りる |
| 正本の指し直し | 「正本は docs/…/認可.md」「検査の中身はdoc-lintスキルが持つ」 | 正本は定めた場所が持つ。指し直せば二重管理になる |
| issue番号・日付・担当者 | `// #178 対応` | issueとgitが持つ。番号は再利用され意味を失う |

- 12.5 責務境界は書いてよい。「このモジュールが担わないこと」は、読み手が探し先を誤らないための情報であって実装詳細ではない
- 12.6 コードを直したらコメントも直す。嘘のコメントは無いより悪い。直すのが面倒なら、それは陳腐化するコメントを書いた証拠なので消す
- 12.7 既にあるコメントを直すのは許可を要らない。嘘になったものを直す、消すのは常にしてよい。増やすときだけ許可が要る
- 12.8 既存のコメントを増やす方向に手を入れない。修正の説明をコメントで足すのは、コミットメッセージに書くべきことをコードに書いている
